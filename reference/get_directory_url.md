# URLs for MDE-ORG directory extracts

Returns the extract URLs for school and district directory data from
MDE-ORG.

## Usage

``` r
get_directory_url(type = "school")
```

## Arguments

- type:

  Either "school" or "district"

## Value

URL string for CSV extract
