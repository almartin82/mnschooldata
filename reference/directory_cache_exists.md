# Check if directory cache exists and is valid

Check if directory cache exists and is valid

## Usage

``` r
directory_cache_exists(end_year, type, max_age = 30)
```

## Arguments

- end_year:

  School year end

- type:

  Cache type

- max_age:

  Maximum age in days (default 30)

## Value

TRUE if valid cache exists
