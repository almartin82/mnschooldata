# Process raw school directory data

Transforms raw MDE-ORG school directory data into a standardized schema.

## Usage

``` r
process_school_directory(raw_data)
```

## Arguments

- raw_data:

  Data frame from get_raw_school_directory

## Value

Processed data frame with standardized columns
