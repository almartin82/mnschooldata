# Download raw district directory data from MDE-ORG

Downloads the district (LEA) directory CSV extract from the MDE
Organization Reference Glossary (MDE-ORG).

## Usage

``` r
get_raw_district_directory()
```

## Value

Data frame with raw district directory data
