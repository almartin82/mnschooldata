# Process raw district directory data

Transforms raw MDE-ORG district (LEA) directory data into a standardized
schema.

## Usage

``` r
process_district_directory(raw_data)
```

## Arguments

- raw_data:

  Data frame from get_raw_district_directory

## Value

Processed data frame with standardized columns
